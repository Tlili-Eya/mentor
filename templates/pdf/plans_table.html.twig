<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    <style>
        body { font-family: Arial, sans-serif; font-size: 10px; }
        h1 { color: #2c3e50; text-align: center; margin-bottom: 20px; }
        table { width: 100%; border-collapse: collapse; margin-top: 10px; }
        th { background-color: #34495e; color: white; padding: 8px; text-align: left; }
        td { padding: 6px; border: 1px solid #ddd; }
        tr:nth-child(even) { background-color: #f9f9f9; }
        .badge { 
            padding: 2px 6px; 
            border-radius: 10px; 
            font-size: 9px; 
            font-weight: bold; 
        }
        .badge-warning { background-color: #f39c12; color: white; }
        .badge-info { background-color: #3498db; color: white; }
        .badge-success { background-color: #27ae60; color: white; }
        .badge-danger { background-color: #e74c3c; color: white; }
        .footer { margin-top: 30px; text-align: right; color: #7f8c8d; font-size: 9px; }
    </style>
</head>
<body>
    <h1>{{ title }}</h1>
    <p>Généré le : {{ date|date('d/m/Y H:i') }}</p>
    {% if filters.search %}
        <p>Recherche : {{ filters.search }}</p>
    {% endif %}
    {% if filters.statut %}
        <p>Statut filtré : {{ filters.statut }}</p>
    {% endif %}
    <p>Total : {{ plans|length }} plans</p>
    
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Décision</th>
                <th>Description</th>
                <th>Catégorie</th>
                <th>Statut</th>
                <th>Date</th>
            </tr>
        </thead>
        <tbody>
            {% for plan in plans %}
            <tr>
                <td>{{ plan.id }}</td>
                <td>{{ plan.decision }}</td>
                <td>{{ plan.description|slice(0, 100) }}{% if plan.description|length > 100 %}...{% endif %}</td>
                <td>
                    {% if plan.categorie %}
                        {{ plan.categorie.value }}
                    {% else %}
                        -
                    {% endif %}
                </td>
                <td>
                    {% set statut_class = {
                        'EN_ATTENTE': 'badge-warning',
                        'EN_COURS': 'badge-info',
                        'FINI': 'badge-success',
                        'REJETE': 'badge-danger'
                    } %}
                    <span class="badge {{ statut_class[plan.statut.value]|default('') }}">
                        {{ plan.statut.value|replace({'_': ' '})|title }}
                    </span>
                </td>
                <td>{{ plan.date ? plan.date|date('d/m/Y') : 'N/A' }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    
    <div class="footer">
        MentorAI - Page 1 sur 1
    </div>
</body>
</html>