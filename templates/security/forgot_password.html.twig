<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mot de passe oublié - MentorAI</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary-light: #9dbbce;
            --primary-dark: #102c59;
            --primary-red: #d52e28;
            --light-color: #f9fafb;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #9dbbce 0%, #7a9aad 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .reset-container {
            width: 100%;
            max-width: 500px;
            animation: fadeInUp 0.6s ease;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .reset-card {
            background: white;
            border-radius: 24px;
            box-shadow: 0 20px 60px rgba(16, 44, 89, 0.25);
            overflow: hidden;
            border: 2px solid rgba(16, 44, 89, 0.1);
        }

        .reset-header {
            background: linear-gradient(135deg, #102c59 0%, #0a1a36 100%);
            padding: 40px 30px;
            text-align: center;
            color: white;
        }

        .reset-header .icon {
            width: 80px;
            height: 80px;
            background: rgba(157, 187, 206, 0.3);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            backdrop-filter: blur(10px);
            border: 3px solid rgba(157, 187, 206, 0.5);
        }

        .reset-header .icon i {
            font-size: 2.5rem;
            color: #9dbbce;
        }

        .reset-header h1 {
            font-size: 1.75rem;
            font-weight: 700;
            margin-bottom: 8px;
            color: white;
        }

        .reset-header p {
            font-size: 0.95rem;
            opacity: 0.9;
            margin: 0;
            color: #9dbbce;
        }

        .reset-body {
            padding: 40px 30px;
        }

        .info-box {
            background: rgba(157, 187, 206, 0.1);
            border-left: 4px solid #102c59;
            padding: 16px;
            border-radius: 8px;
            margin-bottom: 24px;
        }

        .info-box i {
            color: #102c59;
            margin-right: 10px;
        }

        .info-box p {
            margin: 0;
            color: #102c59;
            font-size: 0.9rem;
            line-height: 1.6;
        }

        .alert {
            border-radius: 12px;
            border: none;
            padding: 16px;
            margin-bottom: 24px;
            display: flex;
            align-items: center;
            gap: 12px;
            border-left: 4px solid;
        }

        .alert-success {
            background: rgba(40, 167, 69, 0.1);
            color: #155724;
            border-left-color: #28a745;
        }

        .alert-error {
            background: rgba(213, 46, 40, 0.1);
            color: #d52e28;
            border-left-color: #d52e28;
        }

        .alert i {
            font-size: 1.25rem;
        }

        .form-group {
            margin-bottom: 24px;
        }

        .form-label {
            display: block;
            font-weight: 600;
            color: #102c59;
            margin-bottom: 10px;
            font-size: 0.95rem;
        }

        .form-label i {
            color: #d52e28;
        }

        .input-group-custom {
            position: relative;
        }

        .input-group-custom i {
            position: absolute;
            left: 18px;
            top: 50%;
            transform: translateY(-50%);
            color: #d52e28;
            font-size: 1.1rem;
            transition: color 0.3s ease;
        }

        .form-control {
            width: 100%;
            padding: 14px 18px 14px 50px;
            border: 2px solid #9dbbce;
            border-radius: 12px;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: #f8fafc;
            color: #102c59;
        }

        .form-control:focus {
            outline: none;
            border-color: #102c59;
            background: white;
            box-shadow: 0 0 0 4px rgba(16, 44, 89, 0.1);
        }

        .input-group-custom:has(.form-control:focus) i {
            color: #102c59;
        }

        .btn-submit {
            width: 100%;
            padding: 16px;
            background: linear-gradient(135deg, #d52e28 0%, #b82520 100%);
            border: none;
            border-radius: 12px;
            color: white;
            font-size: 1.05rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 16px rgba(213, 46, 40, 0.3);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(213, 46, 40, 0.4);
            background: linear-gradient(135deg, #b82520 0%, #d52e28 100%);
        }

        .reset-footer {
            padding: 24px 30px;
            background: #f1f5f9;
            text-align: center;
            border-top: 2px solid #9dbbce;
        }

        .reset-footer a {
            color: #102c59;
            text-decoration: none;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s ease;
        }

        .reset-footer a:hover {
            color: #d52e28;
            gap: 12px;
        }

        @media (max-width: 576px) {
            .reset-container {
                max-width: 100%;
            }

            .reset-header, .reset-body {
                padding: 30px 20px;
            }

            .reset-header h1 {
                font-size: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="reset-container">
        <div class="reset-card">
            <!-- Header -->
            <div class="reset-header">
                <div class="icon">
                    <i class="fas fa-key"></i>
                </div>
                <h1>Mot de passe oublié ?</h1>
                <p>Pas de problème, nous allons vous aider</p>
            </div>

            <!-- Body -->
            <div class="reset-body">
                <!-- Info Box -->
                <div class="info-box">
                    <i class="fas fa-info-circle"></i>
                    <p>
                        Entrez votre adresse email et nous vous enverrons un lien pour réinitialiser votre mot de passe.
                        Le lien sera valide pendant 1 heure.
                    </p>
                </div>

                <!-- Messages Flash -->
                {% for message in app.flashes('success') %}
                    <div class="alert alert-success">
                        <i class="fas fa-check-circle"></i>
                        <span>{{ message }}</span>
                    </div>
                {% endfor %}

                {% for message in app.flashes('error') %}
                    <div class="alert alert-error">
                        <i class="fas fa-exclamation-circle"></i>
                        <span>{{ message }}</span>
                    </div>
                {% endfor %}

                <!-- Formulaire -->
                <form method="post" id="resetForm">
                    <div class="form-group">
                        <label class="form-label" for="email">
                            <i class="fas fa-envelope me-2"></i>Adresse email
                        </label>
                        <div class="input-group-custom">
                            <input 
                                type="email" 
                                class="form-control" 
                                id="email" 
                                name="email" 
                                required 
                                autofocus
                                placeholder="votre.email@exemple.com"
                            >
                            <i class="fas fa-envelope"></i>
                        </div>
                    </div>

                    <button type="submit" class="btn-submit">
                        <i class="fas fa-paper-plane"></i>
                        <span>Envoyer le lien de réinitialisation</span>
                    </button>
                </form>
            </div>

            <!-- Footer -->
            <div class="reset-footer">
                <a href="{{ path('app_login') }}">
                    <i class="fas fa-arrow-left"></i>
                    <span>Retour à la connexion</span>
                </a>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>